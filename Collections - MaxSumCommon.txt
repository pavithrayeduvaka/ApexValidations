//Test Script
/*
public class NumberList {

    public Integer FindMax(List<Integer> inputList) {
        

        Integer max = inputList.get(0);

        for (Integer num : inputList) {
            if (num > max) {
                max = num;
            }
        }
        return max;
    }

    public Integer FindSum(List<Integer> inputList) {
        

        Integer sum = 0;
        for (Integer num : inputList) {
            sum += num;
        }
        return sum;
    }

    public List<Integer> findCommonElements(List<Integer> list1, List<Integer> list2) {
        

        Set<Integer> set1 = new Set<Integer>(list1);
        Set<Integer> set2 = new Set<Integer>(list2);
        set1.retainAll(set2); 
        return new List<Integer>(set1); 
    }
}
*/
PLACEHOLDER
public class CustomException extends Exception {}

try {
    NumberList numberList = new NumberList();

    List<Integer> list1 = new List<Integer>{1, 2, 3, 4, 5};
    List<Integer> list2 = new List<Integer>{4, 5, 6, 7, 8};

    Integer max = numberList.FindMax(list1);
    System.assertEquals(5, max);

    Integer sum = numberList.FindSum(list1);
    System.assertEquals(15, sum);

    List<Integer> commonElements = numberList.findCommonElements(list1, list2);
    List<Integer> expectedCommon = new List<Integer>{4, 5};
    System.assertEquals(expectedCommon, commonElements);

} catch (Exception exc) {
    throw new CustomException('There was an error: ' + exc.getMessage());
}
