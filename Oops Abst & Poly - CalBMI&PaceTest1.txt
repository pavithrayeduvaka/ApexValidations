//Test Script
/*
public class Fitness {

public Integer weight;
public Integer height;
public Integer distance;
public Integer timePeriod;
    
    public Decimal calculateBMI() {
    return weight / height;
    }

    public Decimal calculatePace() {
    return distance / timePeriod;
    }
}
*/
PLACEHOLDER
public class CustomException extends Exception {}

try {
    Fitness fs = new Fitness();
    
    fs.weight = 70;
    fs.height = 5;
    fs.distance = 10;
    fs.timePeriod = 1;
    fs.calculateBMI();
    System.assertEquals(14, fs.calculateBMI());
 
    fs.calculatePace();
    System.assertEquals(10, fs.calculatePace());

} catch (Exception exc) {
    throw new CustomException('There was an error: ' + exc.getMessage());
}

