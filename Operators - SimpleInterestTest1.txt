//Test Script
/*
integer pi = 400000;
decimal ri = 0.18;
integer d = 4;
decimal  simpleInterest = pi*ri*d/100;
system.debug(simpleInterest);

*/
PLACEHOLDER
public class CustomException extends Exception{}
try{  
    System.assertequals(2880, simpleInterest);
    system.debug(simpleInterest);
} catch(Exception exc){
    throw new CustomException('there is a error '+exc.getMessage());
}