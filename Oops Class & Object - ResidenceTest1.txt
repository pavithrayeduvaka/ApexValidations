//Test Script
/*
public class Residence {

    public Boolean isKitchenThere;
    public Boolean isHallThere;
    public String typeOfTheHouse;
    public Integer noOfRooms;
public String determineTypeOfHouse() {
        if (isHallThere == true && isKitchenThere == true && noOfRooms == 1) {
            typeOfTheHouse = '1BHK';
        } else if (isHallThere == true && isKitchenThere == true && noOfRooms == 2) {
            typeOfTheHouse = '2BHK'; 
        } else if (isHallThere == true && isKitchenThere == true && noOfRooms == 3) {
            typeOfTheHouse = '3BHK';
        } else {
            typeOfTheHouse = 'We cannot construct this House';
        }
        return typeOfTheHouse;
    }
}
*/
PLACEHOLDER
public class CustomException extends Exception {}

try {
    Residence r = new Residence();
    r.isHallThere = true;
    r.isKitchenThere = true;
    r.noOfRooms = 1;
    r.determineTypeOfHouse();
    System.assertEquals('1BHK', r.determineTypeOfHouse());

} catch (Exception exc) {
    throw new CustomException('There is an error: ' + exc.getMessage());
}
