//Test Script
/*
public class House {

    public Integer areaInSft;
    public String sizeOfHouse;
   
public String displaySize() {
        if (areaInSft <= 1000) {
            sizeOfHouse = 'Small';  
        } else if (areaInSft > 1000 && areaInSft <= 2000) {
            sizeOfHouse = 'Medium';       
        } else{
            sizeOfHouse = 'Large';         
        }
        return sizeOfHouse;
    }

 }

*/
PLACEHOLDER
public class CustomException extends Exception {}

try {
    House h = new House();
    h.areaInSft = 1200;
    System.assertEquals('Medium', h.displaySize());

} catch (Exception exc) {
    throw new CustomException('There is an error: ' + exc.getMessage());
}