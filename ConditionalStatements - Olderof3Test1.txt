//Test Script
/*
integer x = 60;
integer y = 85;
integer z = 90;
integer greatest;
if(x > y && x > z){
greatest = x;
}
else if (y > z && y > x) {
greatest = y;
}
else{
greatest = z;
}
system.debug(greatest);

*/
PLACEHOLDER
public class CustomException extends Exception{}
try{
   
    System.assertequals(90, greatest);  
} catch(Exception exc){
    throw new CustomException('there is a error '+exc.getMessage());
}