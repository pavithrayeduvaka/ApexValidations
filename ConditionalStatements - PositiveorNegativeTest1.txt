//Test Script
/*
integer givenNumber = -40;
string checkValue;
if(givenNumber > 0 ){
checkValue = 'positive';
}
else if (givenNumber == 0) {
checkValue = 'Zero';
}
else{
checkValue = 'negative';
}
system.debug(checkValue);
*/

PLACEHOLDER
public class CustomException extends Exception{}
try{
    string negative = 'negative';
    system.assertEquals(negative, checkValue);

} catch(Exception exc){
    throw new CustomException('there is a error '+exc.getMessage());
}